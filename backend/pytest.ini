[pytest]
testpaths = tests
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
addopts = -q --strict-markers
markers =
    db: requires a reachable PostgreSQL instance
    chromium: renders through a real headless browser instead of the Pillow fallback
filterwarnings =
    ignore::DeprecationWarning
